import {
    JsonController
} from "routing-controllers";
import { Service } from "typedi";
import { OpenAPI } from "routing-controllers-openapi";
import { Summary } from 'routing-controllers-openapi-extra';
{{#if serviceClassName}}
import {{serviceClassName}} from "./{{kebabCaseName}}.service";
{{/if}}
@Service()
@OpenAPI({
    tags: ["{{kebabCaseName}}"],
})
@JsonController("{{kebabCaseName}}")
export class {{ className }} {
    constructor({{#if serviceClassName}}public {{serviceCamelCaseName}}: {{serviceClassName}}{{/if}}) { }
}
