import { AppConfig, createApp } from "@tsdiapi/server";
{{#if plugins}}
{{#each plugins}}
import {{importPackageName}} from "{{packageName}}";
{{/each}}
{{/if}}

{{#if plugins}}
createApp({ 
    plugins: [
    {{#each plugins}}
        {{importPackageName}}(),
    {{/each}}
    ]
});
{{else}}
createApp();
{{/if}}