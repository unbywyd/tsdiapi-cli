import { Operation, PrismaOperation, DbAfterListener, DbBeforeListener} from "@tsdiapi/prisma";
import { Service } from "typedi";
import { Prisma } from "@prisma/client";
import { PrismaOperationArgs, PrismaOperationResults, PrismaEventPayload, PrismaModelNames } from "@base/prisma.types";

@Service()
export class {{className}} {
/*
@Operation(PrismaOperation.Create, Prisma.ModelName['Country'])
public updateDates(
args: PrismaOperationArgs<"Country", PrismaOperation.Create>
    ) {
    // Modify the args here
    return args; // Return the modified args
    }
    */
    }