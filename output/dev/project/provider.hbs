import type { Logger } from "winston";
import { PluginOptions } from "./index";

export class {{pascalCase name}}Provider {
    private config: PluginOptions;
    private logger: Logger;

    constructor(config: PluginOptions, logger: Logger) {
        this.config = config;
        this.logger = logger;
    }

    public init() {
        this.logger.info(`Initializing {{pascalCase name}} provider...`);
        // TODO: Your initialization logic here
    }
}